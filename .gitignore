# Build artifacts
build/
zig-out/
zig-cache/
.zig-cache/
src/board.zig
dist/

# Firmware binaries (produced by tools/release.sh, fetched into web/firmware/
# by the Pages workflow — never committed; only the manifests are tracked)
web/firmware/**/*.bin
web/firmware/version.json

# ESP-IDF generated
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# IDE
.vscode/
.idea/
.opencode/

# OS
.DS_Store
Thumbs.db
